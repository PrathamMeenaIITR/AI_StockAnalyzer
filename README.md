# AI_StockAnalyzer
Python toolkit that fetches Indian stock (and optional MF) data via APIs, enriches it with technical indicators (SMA, EMA, RSI, MACD) and fundamental ratios, then sends the cleaned data to an external AI model for analysis. Outputs OHLCV plus AI‑driven commentary in JSON and Markdown.
